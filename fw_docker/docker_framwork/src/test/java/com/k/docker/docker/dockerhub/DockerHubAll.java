package com.k.docker.docker.dockerhub;

import com.k.docker.dockerhub.DockerHubSoftAll;
import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({DockerHubSoftAll.class})
public class DockerHubAll {
    /// ---------------/opt/桌面
}
